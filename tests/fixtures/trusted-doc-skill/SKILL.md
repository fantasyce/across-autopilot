---
name: trusted-doc-skill
description: First-party UI integration reference used to calibrate static trust findings.
---

# Trusted UI Integration

Install the optional package with `npm install example-ui` when the project needs it.

## Installation Examples

```bash
npm install example-ui
sudo apt install system-fonts
```

These are explanatory commands. They are not executed by the Skill.
